#include "common/event.h"

Event::Event(int _player_id, int _event_id, int _value) :
                                    player_id(_player_id),
                                    event_id(_event_id),
                                    value(_value) {}

